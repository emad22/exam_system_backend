; PHP-FPM override — applies when Apache mod_php is NOT used (shared hosting)
; Needed for exams: audio recordings and multiple PDF uploads in writing questions
upload_max_filesize = 100M
post_max_size = 110M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
