; PHP-FPM override — applies when Apache mod_php is NOT used (shared hosting)
; Needed for proctoring: face/ID images arrive as base64 strings in POST body
upload_max_filesize = 20M
post_max_size = 25M
memory_limit = 256M
max_execution_time = 60
max_input_time = 60
